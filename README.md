# neocities-deploy
~~~
Usage: neocities-deploy [--source=<dir>] [--dest=<url>] [--ignore=<file>]... [-h|--help] [--version]

Options:
    --source <dir>      Source directory. Default to './'.
    --dest <url>        Destination URL. Defaults to root.
    --ignore <file>     File(s) in the Source directory that should not be deployed.
    -h --help           Display help.
    --version           Display version.

~~~
## Example
~~~
neocities-deploy --source public
~~~